# VN profile editor screenshots

- `profile-editor-before.png`: earlier VN UI acceptance capture, before optional
  terminology was added (English, new Mystery profile, synthetic game name).
- `profile-editor-after.png`: current production React editor acceptance capture
  with optional terminology expanded (Chinese, existing Base profile, synthetic
  game name and terminology).

The locale, selected game type, and sample profile differ between these captures;
they are not a pixel-diff comparison. The added UI is the optional terminology
section. These captures contain no game dialogue, credentials or character art.
Native companion card screenshots remain in local diagnostics because the tested
portrait uses a separately installed character pack.
